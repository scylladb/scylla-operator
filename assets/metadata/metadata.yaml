# This file contains metadata for the Scylla Operator.
# It serves as a single source of truth, and its values should propagate to all other places that rely on this information.
operator:
  minOpenShiftVersion: "4.20"
  maxOpenShiftVersion: "4.20"
  # minKubernetesVersion specifies the minimum Kubernetes version that the operator supports.
  # This value is propagated the ClusterServiceVersion and must be a valid semver version in the format "X.Y.Z".
  # Conventionally, we only specify the major and minor versions, and always set the patch version to 0
  minKubernetesVersion: "1.32.0"
  redHatCertificationProjectID: "678641799dbe477195796c15"
operatorTests:
  olm:
    channel: "dev" # Channel to use for OLM tests in CI/CD.
