apiVersion: v1
kind: Service
metadata:
  name: grafana
  annotations:
      prometheus.io/scrape: 'true'
      prometheus.io/port:   '3000'
spec:
  selector:
    app: grafana
  sessionAffinity: None
  ports:
    - port: 3000
      protocol: TCP
      targetPort: http-grafana
  type: LoadBalancer
